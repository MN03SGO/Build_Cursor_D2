# Copia este archivo a .env en local y rellena los valores.
# En Vercel: Settings → Environment Variables → añade estas mismas variables.

# --- Obligatorias para Supabase (backend en Vercel) ---
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_ANON_KEY=tu_clave_anon_publica

# --- Chat: Llama (local) o Gemini (nube) ---
# Si defines OLLAMA_URL, el chat usará Ollama (ej: llama3). Si no, usará Gemini.
# OLLAMA_URL=http://localhost:11434

# --- Opcionales ---
VUDY_API_KEY=
GEMINI_API_KEY=
