---
import Layout from '../layouts/Layout.astro';
---
<Layout title="Plan Premium - Asistente de Estudio">
  <h1>Plan Premium</h1>
  <p class="premium-intro">
    Desbloquea funciones avanzadas para estudiar con más profundidad y mejor seguimiento.
  </p>

  <section class="premium-card highlight">
    <div class="price-block">
      <span class="price-tag">USD $9.99</span>
      <span class="price-period">/ mes</span>
    </div>
    <h2 class="section-title">Todo lo que incluye Premium</h2>
    <ul class="premium-list">
      <li>Respuestas de IA nivel experto con más profundidad.</li>
      <li>Hasta 10 preguntas tipo quiz por sesión.</li>
      <li>Planes de estudio más detallados por materia.</li>
      <li>Acceso prioritario a nuevas funciones.</li>
    </ul>
    <button type="button" class="btn btn-primary btn-cta" id="btn-vudy">Suscribirme</button>
    <div id="vudy-result" class="vudy-result" aria-live="polite"></div>
  </section>

  <section class="premium-card">
    <h2 class="section-title">Explicación con IA</h2>
    <p class="premium-desc">Obtén explicaciones personalizadas de tus temas de estudio usando inteligencia artificial.</p>
    <button type="button" class="btn btn-outline btn-ia" id="btn-ia">Probar explicación</button>
  </section>

  <p class="premium-link"><a href="/" class="link-dashboard">← Volver al inicio</a></p>
</Layout>

<style>
  .premium-card {
    background: var(--card);
    padding: 2rem;
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    margin-bottom: 2rem;
  }
  .premium-intro {
    color: var(--text-muted);
    margin-bottom: 1.5rem;
  }
  .premium-card.highlight {
    border: 1px solid rgba(74, 144, 217, 0.25);
    background: linear-gradient(135deg, rgba(74, 144, 217, 0.12), #fff);
  }
  .price-block {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }
  .price-tag {
    font-size: 2rem;
    font-weight: 700;
    color: var(--accent);
  }
  .price-period {
    color: var(--text-muted);
    font-weight: 600;
  }
  .premium-list {
    margin: 1rem 0 1.5rem;
    padding-left: 1.2rem;
    color: var(--text-muted);
  }
  .premium-desc {
    color: var(--text-muted);
    margin-bottom: 1.25rem;
    line-height: 1.5;
  }
  .btn-ia,
  .btn-cta {
    min-height: 52px;
    padding: 0.85rem 1.75rem;
  }
  .vudy-result {
    margin-top: 1rem;
    padding: 1rem;
    border-radius: 8px;
    font-size: 0.95rem;
    min-height: 2.5rem;
  }
  .vudy-result.success {
    background: #e8f5e9;
    color: #2e7d32;
  }
  .vudy-result.loading {
    background: #e3f2fd;
    color: #1565c0;
  }
  .vudy-result.error {
    background: #ffebee;
    color: #c62828;
  }
  .premium-link {
    margin-top: 2rem;
  }
  .link-dashboard {
    color: var(--accent);
    font-weight: 600;
    text-decoration: none;
  }
  .link-dashboard:hover {
    text-decoration: underline;
  }
</style>

<script>
  document.getElementById('btn-ia')?.addEventListener('click', () => {
    alert('En el producto final, aquí se abriría la función de explicación con IA.');
  });

  document.getElementById('btn-vudy')?.addEventListener('click', async () => {
    const resultEl = document.getElementById('vudy-result');
    if (!resultEl) return;
    resultEl.textContent = 'Iniciando suscripción Premium...';
    resultEl.className = 'vudy-result loading';

    try {
      const res = await fetch('/api/premium/vudy-simulate');
      const data = await res.json();
      resultEl.textContent = data.message || (data.success ? 'Solicitud simulada correctamente.' : 'Error.');
      resultEl.className = data.success ? 'vudy-result success' : 'vudy-result error';
    } catch {
      resultEl.textContent = 'Error al conectar con la simulación de Vudy.';
      resultEl.className = 'vudy-result error';
    }
  });
</script>
