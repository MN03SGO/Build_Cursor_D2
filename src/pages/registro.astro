---
import Layout from '../layouts/Layout.astro';
---
<Layout title="Registrar materia - Asistente de Estudio">
  <h1>Registrar nueva materia</h1>

  <form class="form-registro" id="form-registro">
    <div class="form-group">
      <label for="nombre">Nombre de la materia</label>
      <input type="text" id="nombre" name="nombre" required placeholder="Ej: Matemáticas" />
    </div>
    <div class="form-group">
      <label for="fecha">Fecha del examen</label>
      <input type="date" id="fecha" name="fecha" required />
    </div>
    <div class="form-group">
      <label for="prioridad">Prioridad</label>
      <select id="prioridad" name="prioridad" required>
        <option value="alta">Alta</option>
        <option value="media">Media</option>
        <option value="baja">Baja</option>
      </select>
    </div>
    <button type="submit" class="btn btn-primary btn-submit">Enviar</button>
  </form>

  <p class="form-note">En este prototipo el formulario muestra un mensaje de confirmación. Los datos no se guardan.</p>
  <p class="form-link"><a href="/" class="link-dashboard">← Volver al Dashboard</a></p>
</Layout>

<style>
  .form-registro {
    max-width: 420px;
    background: var(--card);
    padding: 2rem;
    border-radius: var(--radius);
    box-shadow: var(--shadow);
  }
  .btn-submit {
    width: 100%;
    margin-top: 0.5rem;
  }
  .form-note {
    margin-top: 1.5rem;
    color: var(--text-muted);
    font-size: 0.9rem;
  }
  .form-link {
    margin-top: 1rem;
  }
  .link-dashboard {
    color: var(--accent);
    font-weight: 600;
    text-decoration: none;
  }
  .link-dashboard:hover {
    text-decoration: underline;
  }
</style>

<script>
  document.getElementById('form-registro')?.addEventListener('submit', (e) => {
    e.preventDefault();
    const form = e.target as HTMLFormElement;
    const nombre = (form.nombre as HTMLInputElement).value;
    const fecha = (form.fecha as HTMLInputElement).value;
    const prioridad = (form.prioridad as HTMLSelectElement).value;
    alert(`Materia registrada (prototipo):\n\nNombre: ${nombre}\nFecha examen: ${fecha}\nPrioridad: ${prioridad}`);
    form.reset();
  });
</script>
